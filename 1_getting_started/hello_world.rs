// main is the entry point of every Rust application
fn main() {
    // println is called with ! That means we're using a macro
    //A macro is a code that writes more code aka. Metaprogramming
    println!("Hello, world!");
}
